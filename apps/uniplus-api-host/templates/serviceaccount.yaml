{{- if and .Values.uniplusApiHost.enabled .Values.uniplusApiHost.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "uniplusApiHost.serviceAccountName" . }}
  labels:
    {{- include "uniplusApiHost.labels" . | nindent 4 }}
{{- end }}
