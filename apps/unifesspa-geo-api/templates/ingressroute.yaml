{{- if and .Values.unifesspaGeoApi.enabled .Values.unifesspaGeoApi.ingress.enabled -}}
{{- if not .Values.unifesspaGeoApi.ingress.host -}}
{{- fail "unifesspaGeoApi.ingress.host é obrigatório quando ingress.enabled=true (ex.: api-geo.standalone.portaluni.com.br)." -}}
{{- end -}}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "unifesspaGeoApi.fullname" . }}
  labels:
    {{- include "unifesspaGeoApi.labels" . | nindent 4 }}
spec:
  entryPoints:
    - {{ .Values.unifesspaGeoApi.ingress.entryPoint }}
  routes:
    - match: Host(`{{ .Values.unifesspaGeoApi.ingress.host }}`)
      kind: Rule
      services:
        - name: {{ include "unifesspaGeoApi.fullname" . }}
          port: 8080
  {{- if .Values.unifesspaGeoApi.ingress.tls.enabled }}
  tls:
    secretName: {{ .Values.unifesspaGeoApi.ingress.tls.secretName | default (printf "%s-tls" (include "unifesspaGeoApi.fullname" .)) }}
  {{- end }}
{{- end }}
