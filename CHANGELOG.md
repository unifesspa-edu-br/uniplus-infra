# Changelog

Todas as mudanças notáveis deste projeto serão documentadas neste arquivo.

O formato é baseado em [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/),
e este projeto adere ao [Semantic Versioning](https://semver.org/lang/pt-BR/).

## [Não lançado]

### Segurança

- Chart `keycloak-replica` sobe a imagem composta `ghcr.io/unifesspa-edu-br/uniplus-keycloak` de `26.6.4-0` para **`26.7.2-0`**, aplicando a correção do **CVE-2026-18963** (CVSS 9.1) publicada pelo Keycloak. A cobertura do CVE cita `26.6.6` como versão corrigida, mas essa é branch do Red Hat Build of Keycloak e não existe no upstream — o primeiro alvo corrigido upstream é `26.7.2`. Não é bump de patch: é minor do Keycloak (26.6 → 26.7). `appVersion` e `version` do Chart (0.3.1 → 0.3.2) sincronizadas. A tag do `keycloak-config-cli` do `realm-reconcile-job` permanece em `6.5.0-26.5.4` — o adorsys ainda não publicou pareamento para 26.6.x nem 26.7.x — e foi exercitada contra Keycloak 26.7.2 nos dois caminhos que o Job percorre (criação do realm e reconciliação de realm já existente), ambos com sucesso; divergência com a ADR-010 registrada na issue #529. Imagem publicada em `unifesspa-edu-br/uniplus-keycloak-providers#17`. Issue #525.
- Chart `keycloak-replica` sobe a imagem composta `ghcr.io/unifesspa-edu-br/uniplus-keycloak` de `26.6.1-0` para **`26.6.4-0`** (Keycloak 26.6.4 + JAR `cpf-matcher`). O Keycloak 26.6.4 corrige 8 CVEs — incluindo CVE-2026-11800 (authentication bypass via JWT algorithm confusion), CVE-2026-9099 (privilege escalation group-admin → realm-admin) e CVE-2026-9799/9800 (authorization bypass) — além do bug de migração Postgres `keycloak/keycloak#48438` (restart loop ao migrar schema 26.5→26.6). `appVersion` e `version` do Chart (0.3.0 → 0.3.1) sincronizadas. Imagem publicada em `unifesspa-edu-br/uniplus-keycloak-providers#16`. Issue #392.

### Removido

- Chart `apps/uniplus-api-selecao/` e o wiring correspondente em `environments/lab-standalone-single/` (Task #412, PR#424) — revertidos. A topologia real do backend não é "uma API por módulo de negócio": ADR-0097 do `uniplus-api` (accepted 2026-06-26) define 3 executáveis — Host (`Unifesspa.UniPlus.Host`, absorve Selecao+Ingresso+Configuracao+OrganizacaoInstitucional como class libraries co-hospedadas), Geo e Portal. O módulo Selecao não é mais um deployable separado; não há mais chart Helm dedicado a ele. Refs #395.
- Limpeza do repositório para focar no `standalone-compact` como único ambiente operacional (2026-05-19): removidos `environments/lab-{sp1,sp2,pa1}`, `environments/prod-{sp1,sp2,pa1}` e `environments/standalone/` (nunca provisionados ou já destruídos); `provisioning/oci/standalone/` (VMs destruídas) e `provisioning/oci/iad-arm/` (exploração ARM Phoenix abandonada); `docs/SETUP.md`, `docs/VALIDATION-PLAN.md`, `docs/network-matrix.md` (laboratório multi-DC nunca executado); `scripts/bootstrap-lab.sh` e `scripts/teardown-lab.sh`.

### Alterado

- ADRs 001 (3 DCs lógicos) e 007 (Vault auto-unseal Transit em PA1) marcados como **Superseded** por ADR-008 (topologia standalone). ADR-002, 003, 004, 005, 006 e 008+ continuam vigentes — suas decisões ainda valem no `standalone-compact` (stateful fora do K8s, OIDC gov.br, borda externa fora do escopo, systemd, ArgoCD).
- `docs/ARCHITECTURE.md` §2.2 e §5.5.3 reescritos para apresentar o modelo 3-DC como referência futura e o `standalone-compact` como ambiente em produção; anexos atualizados.
- `docs/RUNBOOKS.md` ganha banner identificando §1-§7 como referência histórica do modelo 3-DC e §8-§9 como operação atual do standalone-compact.
- `README.md` reescrito: árvore do repositório, princípios e tabela de documentação refletem o estado atual.
- `CLAUDE.md`, `AGENTS.md` e `GEMINI.md` focados em `standalone-compact`, importando `@docs/visao-do-projeto.md` (padrão do monorepo Uni+).
- CI (`.github/workflows/validate.yml`): matriz `ENVS` reduzida de `lab-*/prod-*/standalone` para `standalone-compact` — único environment que existe no repo agora.
- `.gitignore`: passa a ignorar `provisioning/oci/*/.terraform/`, `*.tfplan`, `*.tfstate*`, `plan-*.bin` e `.playwright-mcp/`.
- Chart `keycloak-replica` passa a consumir a imagem composta `ghcr.io/unifesspa-edu-br/uniplus-keycloak:26.6.1-0` (Keycloak 26.6.1 + JAR `cpf-matcher`) em vez do upstream vanilla `quay.io/keycloak/keycloak:26.6.1`. Standalone passa a validar exatamente o binário que vai pra HML/PROD (production parity). `appVersion` do Chart sincronizada com a tag composta. Issue #192, depende de `unifesspa-edu-br/uniplus-keycloak-providers#13` (Release `v26.6.1-0` em 2026-05-10).
- `provisioning/oci/standalone/` passa a codificar em OpenTofu as 2 VMs (`oci_core_instance.k8s_host` + `oci_core_instance.data_host`) e os 4 block volumes do data-host (`oci_core_volume.data` for_each + `oci_core_volume_attachment.data`). Recursos vivos importados via `tofu import` (10 imports); `tofu plan` retorna `No changes` pós-import (estado declarativo bate com OCI). Permite `tofu apply` para mudar perfil de capacidade ou aumentar tamanho dos blocks via diff de tfvars. Aumentar `volume_sizes_gbs` é operação in-place online (OCI suporta scale-up de block volume sem reboot, requer apenas resize do FS no host pós-apply); diminuir não é suportado pela OCI (apenas destroy + apply recria menor). Recursos de rede (VCN/subnets/IGW/NSGs) ainda referenciados por OCID em variáveis — migração na Story #53. Stories #52 + #54 + #55 da Feature #43.
- `provisioning/oci/standalone/network.tf` codifica em OpenTofu a VCN (`oci_core_vcn.this`, `10.0.0.0/16`), o Internet Gateway, o NAT Gateway (egress da subnet privada), as 2 route tables (pública→IGW, privada→NAT), as 2 security lists (pública: 22/80/443 do mundo; privada: 22/5432/6379/9000-9001/9092/ICMP type 3 code 4 da própria VCN) e as 2 subnets (pública 10.0.1.0/24, privada 10.0.2.0/24). 9 recursos importados; `tofu plan` retorna `No changes` pós-import. Variáveis OCID externas (`vcn_ocid`, `subnet_*_ocid`) substituídas por CIDRs (`vcn_cidr`, `subnet_cidrs`) — defaults preservam a topologia viva. Default route table + default security list do VCN ficam não-gerenciados (não anexados a subnet alguma). NSGs (granularidade por VNIC) ficam para refinamento futuro — security lists por subnet atendem hoje. Story #53 da Feature #43.
- `provisioning/oci/standalone/public_ip.tf` codifica em OpenTofu o Reserved Public IP do k8s-host (`oci_core_public_ip.k8s_host`, `lifetime = "RESERVED"`). Anexado via `data.oci_core_private_ips` ao primary VNIC do k8s-host. IP `164.152.53.29` (já era RESERVED no recurso vivo — operação manual passada promoveu o ephemeral). Sobrevive a `tofu destroy && tofu apply` — recriar a VM mantém o mesmo IP, evitando reconfigurar DNS, gov.br callback URL, certs Let's Encrypt e `KC_HOSTNAME`. Custo: $0/h enquanto attached a VM running. Parte da Story #56 da Feature #43.
- `scripts/sync-tofu-outputs.sh` (novo) é a bridge entre `tofu output` (provisioning/oci/standalone) e os charts Helm consumidos via ArgoCD. Modos: tabela (default — lista outputs + mapeamento para campos do `environments/standalone/values.yaml`), `--diff` (verifica se IPs/FQDN do Tofu estão sincronizados com o values.yaml; sinaliza drift cedo), `--apply-configmap` (cria/patcha `ConfigMap` K8s `standalone-tofu-outputs` no namespace `uniplus` com todos os outputs como chaves planas — charts consomem via `envFrom`/`valueFrom`, útil para Story #57 quando KMS auto-unseal destravar), `--json`. Estratégia documentada no README do diretório `provisioning/oci/standalone`: schema do `values.yaml` continua provider-agnostic (em git, ArgoCD-readable); valores OCI-specific ficam hardcoded com `--diff` detectando drift; outputs sensíveis ou OCID-only não entram em `values.yaml`, ficam no ConfigMap. Story #58 da Feature #43.
- `provisioning/oci/standalone/dns.tf` codifica em OpenTofu os 11 DNS records do subdomínio `*.standalone.portaluni.com.br` na zona OCI DNS `portaluni.com.br` (`oci_dns_rrset.standalone_apex` apontando para o Reserved Public IP + `oci_dns_rrset.standalone_cname` com `for_each` cobrindo `api-ingresso`, `api-portal`, `api-selecao`, `ingresso`, `kafka-ui`, `minio`, `portal`, `redis-ui`, `schema-registry`, `selecao` apontando para o apex). Adicionar novo serviço Traefik = adicionar entry em `local.standalone_cnames` + `tofu apply`. Issue #208 (que parecia bug do provider OCI v7.32) foi closed após investigação — o problema era sintaxe de import: o formato correto é `zoneNameOrId/<id>/domain/<dom>/rtype/<rt>` (key/value), não `<id>/<dom>/<rt>` (slash simples). Documentado no README. Story #56 da Feature #43 (parte DNS).

### Corrigido

- `description` do client `grafana` em `apps/keycloak-replica/files/uniplus-realm.json` encurtada de 362 para 227 caracteres. Mesma causa raiz do #196 (`CLIENT.DESCRIPTION` do Keycloak é `VARCHAR(255)`) — o client `grafana` foi adicionado depois daquele fix (SSO do Grafana) e não passou pelo mesmo audit. Diferente do #196 (que só quebrava no `UPDATE` via `keycloak-config-cli`), aqui o `--import-realm` já falhava no `INSERT` inicial — o pod do Keycloak entrava em `CrashLoopBackOff` na primeira subida, antes do Job `realm-reconcile` sequer rodar. A versão encurtada também remove o placeholder `${GRAFANA_CLIENT_SECRET}` do texto: como o Job `realm-reconcile` roda com `IMPORT_VARSUBSTITUTION_ENABLED=true` (substituição recursiva em qualquer campo do JSON, não só em `secret`), manter o placeholder na `description` faria o valor real do secret ser persistido em texto plano nesse campo, exposto a qualquer usuário com acesso de leitura aos metadados do client. Validado com reimport limpo (delete do realm via Admin API + restart do pod) contra Postgres 18 + PostGIS 3.6 e imagem `ghcr.io/unifesspa-edu-br/uniplus-keycloak:26.6.4-0`: `Realm 'uniplus' imported` / `Import finished successfully`; `description` persistida conferida via Admin API. Issue #394.
- Chart `clamav-scanner` ganha emptyDir mountado em `/var/lock` no container `clamd`. O entrypoint da imagem oficial cria `/var/lock/lock` (lockfile compartilhado) — `/var/lock` no FS image é `root:root` e o container roda como uid 100 → `Permission denied`. Cobre o último gap de paths writable que faltava (`/tmp`, `/run/clamav`, `/var/log/clamav` já tinham emptyDir). Issue #202.
- Chart `clamav-scanner` ganha `initContainer` `chown-signatures` rodando como root (`runAsUser: 0`, capability `CHOWN` only) que faz `chown -R 100:100 /var/lib/clamav` antes do container principal subir. O fix anterior (`fsGroupChangePolicy: Always` em #198) ficou ineficaz: o kubelet pula `fsGroup` operations para volumes hostPath-based por design — e o standalone usa StorageClass `standalone-local-nvme` (provisioner `rancher.io/local-path`, hostPath) — então o ownership do PV permanecia `0:0`, e o entrypoint da imagem oficial ainda crashava com `Operation not permitted` ao tentar chown como uid 100. Pod entra `Running 1/1` agora. Issue #200.
- Os 6 clients OIDC do realm `uniplus` (`uniplus-portal`, `kafka-ui`, `apicurio-registry`, `uniplus-api-{portal,selecao,ingresso}`) ganham o scope `basic` em `defaultClientScopes`. A partir do Keycloak 25, o claim `sub` no access_token foi movido do código built-in para o protocolMapper `oidc-sub-mapper` que vive dentro do client scope `basic`. Como os 6 clients declaravam `defaultClientScopes` explicitamente (sem incluir `basic`), o JSON sobrescrevia o default do realm e os tokens emitidos vinham sem `sub` — o `/api/auth/me` das APIs Uni+ retornava `userId: null` (descoberto durante smoke #191). Issue #187.
- Chart `clamav-scanner` ganha `fsGroupChangePolicy: Always` no `podSecurityContext`. Sem isso, em ambientes com k3s `local-path` provisioner (standalone), o `fsGroup: 100` declarado não era aplicado ao volume montado em `/var/lib/clamav` — o entrypoint da imagem oficial `clamav/clamav:1.4` tentava `chown` antes de fazer drop privileges para uid 100 e falhava com `Operation not permitted` sob `runAsNonRoot: true`, deixando o pod em `CrashLoopBackOff` desde o merge do PR #182. Issue #188.
- `description` dos 5 clients OIDC em `apps/keycloak-replica/files/uniplus-realm.json` (kafka-ui, apicurio-registry, uniplus-api-portal/selecao/ingresso) encurtada para ≤ 255 chars cada. Causa raiz: o schema da tabela `CLIENT` do Keycloak define `DESCRIPTION` como `VARCHAR(255)` desde KC 1.x; `--import-realm` trunca silenciosamente no INSERT inicial, mas `PUT /admin/realms/{r}/clients/{id}` (UPDATE via admin API — usado pelo `keycloak-config-cli`) rejeita com PG `22001 (value too long for type character varying(255))` → reconciliação falha em HTTP 500. Detalhes operacionais longos (procedure `kcadm.sh` para recuperar `client_secret`, semântica dos mappers) movidos para `RUNBOOKS.md`. Issue #196.
- `NetworkPolicy` do chart `keycloak-replica` ganha 3ª regra de ingress permitindo pods com label `uniplus.io/job-role=realm-reconcile` (mesmo namespace) chamarem o KC na porta HTTP. Sem isso, o Job `realm-reconcile` (ADR-010) — embora rodando vizinho ao KC — não conseguia alcançar o Service e morria em timeout/`backoffLimit`. Bug pré-existente do PR #183, mascarado anteriormente pelo `ImagePullBackOff` da issue #192. Escopo cirúrgico (label específica, não abre ingress geral do namespace). Issue #194.
- Tag de `quay.io/adorsys/keycloak-config-cli` em `apps/keycloak-replica/values.yaml` corrigida de `6.4.0-26.0.6` (inexistente — typo histórico do PR #183) para `6.5.0-26.5.4` (mais recente publicada; mesma major do KC vivo, admin API estável). Sem essa correção o Job `realm-reconcile` (ADR-010) ficava em `ImagePullBackOff` indefinidamente, bloqueando reconciliação automática do realm em re-deploy. Issue #192.
- Keycloak client `uniplus-portal` (public + PKCE, frontend Angular) recebe `protocolMapper` `audience-uniplus` (`oidc-audience-mapper` com `included.custom.audience=uniplus`, projetado apenas no access_token). Sem o mapper o token vinha com `aud: ["account"]` e as APIs Uni+ — que validam `Auth__Audience=uniplus` — respondiam 401 a qualquer chamada do frontend. Mitigação manual via `kcadm.sh` foi aplicada durante o smoke #166; agora o mapper está persistido em `apps/keycloak-replica/files/uniplus-realm.json` e o Job `realm-reconcile` (ADR-010, post-install/post-upgrade) o reaplica em re-deploy. Issue #186.

### Adicionado

- `docs/RUNBOOKS.md` ganha a seção **20. Ambiente de laboratório — host único (`lab-standalone-single`)**, cobrindo topologia (1 VM combinando os papéis `k8s-host`+`data-host` do `standalone-compact`), uso do `scripts/lab-standalone-single/bootstrap.sh` consolidado, troubleshooting (symlink `kubectl`/`KUBECONFIG` do K3s, conflito de field-manager no `helm upgrade vault`, `Kafka__BootstrapServers` sempre renderizado), procedimento manual de deploy de `uniplus-api-host`/`uniplus-api-portal` (sem imagem publicada em GHCR ainda) e tabela de diferenças em relação ao `standalone-compact`. Fecha o critério de aceite original da issue #395 (Feature #401, última Task da decomposição). Issue #416.
- `environments/lab-standalone-single/` — values overlay de referência para a topologia de laboratório de host único (K3s + Docker na mesma VM), aplicado manualmente (não via ApplicationSet/GitOps) durante a implementação da issue #395. Primeiro componente coberto: `platform/vault/` (Vault single-node, `replicas=1`, Shamir manual simplificado — 1 key share, threshold 1 — sem peer PA1 nem OCI KMS, mesmo `service_registration` removido documentado em `standalone-compact` por bug conhecido com K3s/flannel). Validado de ponta a ponta na VM de lab: pod `Running`/`1/1 Ready`, `vault operator init` + `unseal` bem-sucedidos, `vault status` confirma `Sealed: false` e `HA Mode: active`. Cresce incrementalmente nas próximas Tasks da decomposição (#395).
- `environments/lab-standalone-single/` ganha `platform/external-secrets/` — ESO + `ClusterSecretStore vault-default` apontando para o Vault já provisionado (Task #409), auth Kubernetes habilitado no Vault com policy `external-secrets-read` (read-only em `secret/data/*` e `secret/metadata/*`) e role `external-secrets` vinculada à ServiceAccount do operator. `networkPolicy.enabled` passa de `false` para `true` na raiz do `values.yaml` do environment (afeta os dois charts, Vault e ESO, que compartilham essa chave) com `kubeApiCidrs` incluindo o CIDR do host da VM além do service CIDR — o k3s do lab enforça NetworkPolicy via `kube-router` mesmo usando flannel como dataplane (suposição anterior em contrário estava incorreta), e o egress ao Service `kubernetes` é avaliado após o DNAT do kube-proxy, então o CIDR de serviço sozinho não cobre o pacote já reescrito para `<ip-do-host>:6443` (mesmo bug já documentado em `environments/standalone-compact/values.yaml` para a VCN OCI). Instalação em duas etapas documentada no README do environment: CRDs do ESO vêm como templates normais do subchart (não a pasta `crds/` especial do Helm), então não podem coexistir com o `ClusterSecretStore` no primeiro `helm install`. Validado de ponta a ponta na VM de lab: os 3 pods do ESO (controller, webhook, cert-controller) `Running`/`1/1 Ready`, `ClusterSecretStore vault-default` com status `Valid`/`Ready: True`, `ExternalSecret` de teste sincronizou corretamente um Secret K8s a partir de um valor escrito no Vault. Issue #410.
- `scripts/lab-standalone-single/seed-vault-secrets.sh` — popula no Vault do lab os paths que os charts `uniplus-api-*` e `keycloak-replica` esperam (`secret/standalone/{redis/default,minio/root,kafka/admin}` e `secret/standalone/keycloak/clients/uniplus-api-{selecao,portal,ingresso}`), sincronizando a partir das fontes de verdade já vivas na VM (`.bootstrap-creds` de cada serviço de dado + `kcadm.sh` no pod `keycloak-replica` para os client_secrets M2M) em vez de gerar segredo novo. `secret/standalone/postgres/{selecao,portal,ingresso}` deliberadamente fora de escopo — cada role+db dedicado só existe a partir da Task que sobe a respectiva API (#412/#413/#414), populado dentro daquele procedimento. Idempotente por natureza (`vault kv put` sempre reflete a fonte de verdade atual, útil para ressincronizar após rotação de um client_secret). Validado de ponta a ponta na VM de lab: os 6 paths escritos, `ExternalSecret`s de teste equivalentes aos consumidos pelos charts reais sincronizaram (`SecretSynced`/`Ready: True`) com os valores exatos esperados, incluindo o CA cert multi-linha do Kafka. Issue #411.
- `environments/lab-standalone-single/` ganha `apps/uniplus-api-portal/` — deployable autônomo (ADR-0097 do `uniplus-api`, accepted 2026-06-26): a topologia real do backend não é "uma API por módulo de negócio" — é 3 executáveis: **Host** (`Unifesspa.UniPlus.Host`, absorve Selecao+Ingresso+Configuracao+OrganizacaoInstitucional como class libraries co-hospedadas, banco único `uniplus` schema-por-módulo), **Geo** e **Portal**. Role+database Postgres dedicados (`portal`/`uniplus_portal`), secret no Vault, LocalKey via Secret K8s manual (não versionado — chart não injeta isso automaticamente, sem vaultPath default), `aspnet.environment: Development` (sem Traefik/TLS no lab, Keycloak in-cluster só HTTP, API rejeita `Auth:Authority` sem HTTPS fora de Development), `kafka.enabled: false` (`SchemaRegistrationHostedService` fail-fast no boot exige Schema Registry alcançável quando Kafka está populado — issue #423 rastreia religar quando o Apicurio entrar no lab). O módulo Portal ainda não tem entidades de domínio implementadas no código — `dotnet ef` não gera migrations pendentes (`Nenhuma migration EF Core pendente para PortalDbContext`), o banco fica só com o schema `wolverine` (infraestrutura de mensageria), sem tabelas de negócio. `/health/live` respondendo `Healthy`. Issue #413.
- `apps/uniplus-api-host/` (chart novo) — composition root do monólito modular (ADR-0097 do `uniplus-api`): hospeda os módulos internos Selecao+Ingresso+Configuracao+OrganizacaoInstitucional num único processo (`Unifesspa.UniPlus.Host`), banco Postgres único `uniplus` (role `uniplus`) com schema-por-módulo — as 5 connection strings do host (`UniPlusDb`, `ConfiguracaoDb`, `OrganizacaoDb`, `SelecaoDb`, `IngressoDb`) apontam todas para o mesmo host/database/role. Substitui os charts `apps/uniplus-api-{selecao,ingresso}/`, revertidos por representarem a topologia anterior (issue #412/#414). Diferença de design deliberada em relação aos charts `uniplus-api-{selecao,portal}`: `Kafka__BootstrapServers` é sempre renderizado (nunca omitido), com valor `" "` (espaço) quando `kafka.enabled=false` — o host consulta essa config via `string.IsNullOrWhiteSpace` em dois pontos do `uniplus-api`, que trata espaço como vazio, desligando o transporte Kafka de forma limpa sem disparar a validação fatal do Selecao (ADR-0051) nem deixar o SDK Confluent.Kafka cair no default `localhost:9092` em loop de reconexão eterno (bug real descoberto na Task #412, revertida). Sem imagem publicada em GHCR ainda (`publish-images.yml` só dispara em tags `v*.*.*`, nenhuma criada; publicar teria efeito real em produção, fora do escopo desta Task) — imagem buildada localmente via `docker/Dockerfile.host` do `uniplus-api` e importada direto no containerd do k3s via `k3s ctr images import` (`pullPolicy: Never`). Validado de ponta a ponta na VM de lab: pod `Running`/`1/1 Ready` já na primeira tentativa, `/health`, `/health/live` e `/health/ready` respondendo `Healthy`, migrations EF Core aplicadas nos 4 schemas dedicados (`configuracao`: 15 tabelas, `selecao`: 20, `ingresso`: 5, `organizacao`: 5, além de `wolverine`: 8), rotas `/api/organizacao/unidades` e `/api/configuracao/campi` respondendo `200 []`. Issue #427.
- `scripts/lab-standalone-single/bootstrap.sh` — consolida as Features #398 (Redis/MinIO/Kafka) e #399 (Vault + ESO) num único script idempotente para o host combinado de lab, delegando para os scripts individuais já formalizados (`setup-{redis,minio,kafka}.sh`, `seed-vault-secrets.sh`) e adicionando os steps que ainda eram só manuais/documentados em README: instalação de Docker+K3s+Helm, setup do Postgres (`postgis/postgis:18-3.6`, mesmo padrão systemd/LoadCredential de `bootstrap-standalone.sh`), deploy+init+unseal do Vault, configuração de auth Kubernetes + policy/role, deploy do ESO em duas etapas. Não cobre o deploy das APIs de negócio (`uniplus-api-host`/`uniplus-api-portal`) — decisão de escopo documentada no cabeçalho do script, cada uma depende de artefatos externos ao repositório (build de imagem, role+db dedicados) fora do fluxo "do zero numa VM nova". Descoberta durante a validação: `/usr/local/bin/kubectl` no K3s é symlink para o binário `k3s` unificado, que usa `/etc/rancher/k3s/k3s.yaml` (root:root) como kubeconfig default nativo — ignora `~/.kube/config` mesmo correto, a menos que `KUBECONFIG` seja exportado explicitamente; sem isso todo `kubectl`/`helm` do script falhava com "permission denied" assim que o K3s já estivesse instalado. Validado de ponta a ponta na VM de lab: `--dry-run` completo limpo, execução real (com tudo já instalado/configurado) idempotente do início ao fim — Docker/K3s/Postgres/Redis/MinIO/Kafka reconhecidos e preservados sem restart, Vault e ESO atualizados via `helm upgrade` sem downtime (`Sealed: false`, `HA Mode: active` mantidos), secrets ressincronizados no Vault com sucesso. `shellcheck` limpo. Revisão Codex apontou e corrigiu 6 findings sobre o fluxo "do zero" que a validação manual (ambiente já configurado) não exercitava: exit code 2 do `vault status` sealed/uninitialized se misturando com o fallback de erro sob `pipefail`; `sh -c` remoto sem `set -e` mascarando falha real de `auth enable`/`policy write`; regeneração silenciosa da senha do Postgres quando `.bootstrap-creds` está ausente mas o cluster já foi inicializado; seed de secrets rodando incondicionalmente antes do Keycloak existir; bind mount vazio escondendo o script de init que a própria imagem `postgis/postgis` embute (cluster subia sem PostGIS configurado); reconciliação de policy/role do Vault pulada quando o auth mount já existia mas uma execução anterior falhou antes de completar. Issue #415.
- Chart `apps/unifesspa-geo-api` — API .NET 10 de localidades/CEP/georreferenciamento (módulo Geo, extraído para repo próprio `unifesspa-geo-api`), imagem `ghcr.io/unifesspa-edu-br/unifesspa-geo-api:v1.0.1`. Segue o padrão dos charts `uniplus-api-*` (Postgres/PostGIS dedicado, Redis, OIDC bearer validation, cifragem `Geo:Encryption` local ou Vault Transit, NetworkPolicy, IngressRoute opcional), mas sem Kafka/MinIO/SchemaRegistry — a API Geo não usa essas dependências. `externalSecrets.enabled` é desligável (mesmo padrão do `keycloak-replica`) para ambientes sem Vault/ESO. Validado de ponta a ponta contra Postgres 18 + PostGIS 3.6, Redis 8.6.3 e Keycloak (`ghcr.io/unifesspa-edu-br/uniplus-keycloak:26.6.4-0`) reais: migrations EF Core aplicadas (18 tabelas), `/health`/`/health/live`/`/health/ready` respondendo `Healthy`. Issue #395.
- `scripts/lab-standalone-single/setup-redis.sh` — formaliza o setup do Redis 8.6.3 (validado manualmente antes) para a topologia de laboratório de host único (K3s + data services na mesma VM), complementar ao `standalone-compact`. Replica fielmente o padrão de `.bootstrap-creds` + ACL file + systemd unit já usado no data-host real (`scripts/bootstrap-standalone.sh`), com `DATA_HOST_IP` auto-detectado (a VM de lab é seu próprio "data-host"). Idempotente, com `--dry-run`. Primeira Task da decomposição da issue #395 (Epic do ambiente de teste institucional em host único).
- `scripts/lab-standalone-single/setup-minio.sh` — MinIO via Docker+systemd no host combinado, replicando fielmente `step_data_setup_minio`/`step_data_bootstrap_minio_buckets` do `scripts/bootstrap-standalone.sh` — já é single-node single-drive (SNSD), sem adaptação de topologia. Credenciais via `.bootstrap-creds` (nunca `admin`/`minioadmin`), console (9001) não exposto externamente. Cria os buckets baseline (`keycloak-backups`, `loki-chunks`, `tempo-traces`, `app-uploads`) + `uniplus-storage` (bucket compartilhado esperado pelos charts `uniplus-api-*`). Validado de ponta a ponta na VM de lab (deploy + idempotência). Segunda Task da decomposição da issue #395.
- `scripts/lab-standalone-single/setup-kafka.sh` — Kafka 4.2.0 KRaft via Docker+systemd no host combinado, replicando fielmente `step_data_setup_kafka` do `scripts/bootstrap-standalone.sh` — já é single-node combined (`process.roles=broker,controller`), SASL_SSL + SCRAM-SHA-512 + StandardAuthorizer conforme ADR-009. Gera certificado TLS self-signed (10 anos) e faz o format inicial do storage com `--add-scram` (único momento em que a credencial SCRAM do admin pode ser embarcada) antes do primeiro `systemctl start`. Validado de ponta a ponta na VM de lab: deploy limpo (cert gerado, storage formatado, broker respondendo SASL_SSL) e idempotência confirmada na segunda execução (creds, certs e serviço preservados, sem reformatar). Terceira Task da decomposição da issue #395 — fecha a Feature #398 (componentes de dados restantes).
- Script `scripts/resize-standalone-oci.sh` que muda os shapes OCPU + RAM das 2 VMs OCI do standalone via OCI CLI (`compute instance update --shape-config`). Perfil `poc` (default) aplica `2 OCPU / 12 GB` no k8s-host e `1 OCPU / 4 GB` no data-host — economia de ~$215/mês (75%) sobre o shape original `4 OCPU / 32 GB` em ambos. Perfil `hml` para load real. **Operação reboota as VMs** (~30-90s downtime cada — `compute instance update --shape-config` é Reshape no OCI, não live resize); script pede confirmação interativa por padrão (flag `--yes` para CI). Stateful services (Postgres/Kafka/Redis/MinIO/Vault) e pods K8s religam graciosamente via systemd e auto-recovery do k3s — validado em uso real (10/05). Footprint dimensionado a partir de medição de uso real (k8s-host 9 GB / data-host 2.1 GB) com margem de 25-100%.
- Keycloak realm `uniplus` ganha 3 confidential clients para fluxo M2M `client_credentials` — `uniplus-api-portal`, `uniplus-api-selecao`, `uniplus-api-ingresso`. Cada um com `serviceAccountsEnabled=true`, audience mapper para `apicurio-registry` e hardcoded-claim mapper que projeta `groups: ["/users/uniplus"]` (Apicurio mapeia → role `sr-developer`). Destrava `uniplus-api#358` (integração Apicurio Schema Registry). RUNBOOKS §15.6 documenta procedure de recuperação do `client_secret` pós-import. Issue #163.
- Estrutura inicial do repositório uniplus-infra
- Documentação institucional (README, CONTRIBUTING, SECURITY, LICENSE)
- Documentação técnica (ARCHITECTURE, VALIDATION-PLAN, SETUP, RUNBOOKS)
- 6 diagramas C4 (Context, Container, Deployment, Component, 2 Sequence) com fontes PlantUML
- Estrutura de Helm charts em `apps/` e `platform/`
- Configuração de componentes stateful em `data/`
- Ambientes lab-sp1, lab-sp2, lab-pa1, prod-sp1, prod-sp2
- ArgoCD ApplicationSet + AppProject com RBAC
- Scripts de bootstrap, validação e teardown do laboratório
- GitHub Actions CI (yamllint, helm lint, shellcheck, markdown lint)
- Templates de Issue e Pull Request
